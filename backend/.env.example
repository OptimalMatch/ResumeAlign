# AWS Credentials for Bedrock (required for AI-powered optimization)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1

# MongoDB Configuration
# Note: When running with docker-compose, use the internal service name 'mongodb'
# For external connections, use localhost:27019
MONGODB_URL=mongodb://mongodb:27017
MONGODB_DB_NAME=resume_optimizer

# Note: Make sure your AWS credentials have permission to access Amazon Bedrock
# and the anthropic.claude-3-sonnet model in your specified region